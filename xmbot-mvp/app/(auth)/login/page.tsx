import { LoginForm } from "@/components/auth/login-form"

export const metadata = {
  title: "Sign In - XMBot",
  description: "Sign in to your XMBot trading account",
}

export default function LoginPage() {
  return <LoginForm />
}
